<% import com.excilys.ebi.gatling.charts.report.GroupContainer %>
<%@ val group: GroupContainer %>
<% attributes("request") = group.value.get._2 %>${include("stats_request.tsv.ssp")}
<% group.groups.values.foreach { group => attributes("group") = group %>${include("stats_group.tsv.ssp")}<% } %>
<% group.requests.foreach { request => attributes("request") = request %>${include("stats_request.tsv.ssp")}<% } %>
