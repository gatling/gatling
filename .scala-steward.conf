commits.message = "chore: update ${artifactName} from ${currentVersion} to ${nextVersion}"

updates.pin = [
  { groupId = "org.scala-lang", artifactId="scala-library", version="2." },
  { groupId = "net.sf.saxon", artifactId="Saxon-HE", version="12." },
  { groupId = "org.elasticsearch", artifactId="elasticsearch-tdigest", version="8.15." }
]
