/* URLs */
val base = "http://localhost:8080/excilys-bank-web"
val urlLoginGet = "/public/login.html"
val urlLoginPost = "/login"
val urlHome = "/private/bank/accounts.html"
val urlLogout = "/logout"

val urlBase = httpConfig.baseURL(base)

/* Feeder */
val usersInfos = new TSVFeeder("bank2", List("username", "password"))

/* Scenarios */
include("users")

/* Configuration */
val scnConf = scn.configure feeder(usersInfos) users(1200) ramp(300) protocolConfig(urlBase)

/* Simulation */
runSimulations(scnConf)
