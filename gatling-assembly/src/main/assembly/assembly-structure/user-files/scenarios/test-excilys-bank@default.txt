/* URLs */
val config = httpConfig.baseURL("http://localhost:8080/excilys-bank-web")

/* Feeder */
val usersInfos = new TSVFeeder("bank2", List("username", "password"))

/* Scenarios */
include("users")

/* Configuration */
val scnConf = scn.configure feeder(usersInfos) users(1200) ramp(300) protocolConfig(config)

/* Simulation */
runSimulations(scnConf)
