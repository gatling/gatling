val iterations = 10
val pause1 = 1
val pause2 = 2
val pause3 = 3

val baseUrl = "http://localhost:3000"

val usersInformation = new TSVFeeder("user_information")

val httpConf = httpConfig.baseURL(baseUrl)

include("admin-user-test-gatling")

runSimulations(
    adminUser.configure.users(5).feeder(usersInformation)//.protocolConfig(httpConf)
)